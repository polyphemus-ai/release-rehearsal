---
"@polyphemus/daemon": patch
---

Routines that belong to no project have a home: Setup lists the install's own, with when each runs next and a Run now, beside the ones a project keeps on its own tab. Each is still on the page of the agent that runs it. And the rainbow an agent wears while it works goes round it: one comet running along the near side of a belt, ducking behind the mark and coming back the other side, instead of a gap sliding across.
