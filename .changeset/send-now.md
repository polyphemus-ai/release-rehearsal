---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

A held message can be sent now instead of waiting: it goes next, on its own, and what's working stops to let it — the app says so before it asks.
