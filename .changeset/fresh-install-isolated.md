---
"@polyphemus/core": patch
"@polyphemus/cli": patch
---

A new install stays Isolated after its first thread (it was switched to On this computer on its second start), and `polyphemus -p` exits when its reply is done. Both found by Helm, Polyphemus's default agent.
