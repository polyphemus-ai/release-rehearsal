---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

An agent can propose a routine as yours rather than the project's, a change to a routine that exists, or stopping one; each waits for you to accept, and the old version goes to the trash. A read-only thread's refusals now say they're read-only, not something broken.
