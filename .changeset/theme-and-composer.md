---
"@polyphemus/daemon": patch
---

A theme switcher in the top right: System, Light or Dark, kept per device and applied before the app draws, so there's no flash. It's also under Setup → You. The message box sits on a blurred bar the conversation shows through, rather than a solid block, and the "Newest" button is centred on it.
