---
"@polyphemus/daemon": patch
---

An agent's revision of a skill it already has can be kept: accepting it asks whether to replace the one that's there, which is moved to the trash rather than deleted. It used to fail after you pressed Accept, with no way on.
