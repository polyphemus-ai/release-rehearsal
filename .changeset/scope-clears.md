---
"@polyphemus/daemon": patch
---

Clearing the project chip on Home really clears it. Beside an open project the list is Home, and every redraw re-applied that project's filter, so the chip came back as soon as anything else changed.
