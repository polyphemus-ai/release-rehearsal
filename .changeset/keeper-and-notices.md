---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

Every agent knows which teammate can make and change agents, skills and routines, and asks them instead of asking you. Polyphemus's own notices say where to change model in the app instead of pointing at `/model`, which only works in the terminal.
