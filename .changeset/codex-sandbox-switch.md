---
"@polyphemus/core": minor
"@polyphemus/daemon": minor
---

On a computer where Codex's sandbox can't start, the owner can turn it off on Codex's card in Models & providers: off by default, confirmed first, and said in each thread where Codex then runs commands with the owner's full permissions. Read-only work keeps its sandbox. How long a quota error with no reset time keeps a provider out is now a setting on Defaults & fallback (routing.quota_retry_minutes, 60 minutes unless changed).
