---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

YOLO stays on. It was held only in memory, so Polyphemus restarting — an update, a reboot — quietly went back to asking about everything. It's now kept with the thread, and a thread that isn't open says what it's set to.
