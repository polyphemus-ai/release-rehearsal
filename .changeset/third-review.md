---
"@polyphemus/core": minor
"@polyphemus/daemon": minor
"@polyphemus/cli": minor
---

From a third review pass. Outside every project, a message from anyone but the owner is delivered, never answered by a model, however few people are left in the conversation; a conversation needs someone else in it. Saying yes to an agent's "bring in @someone" follows the same rule as bringing one in by hand. Showing a file as an artifact, making, drafting or deleting a project agent, installing or removing a library skill in a project, setting up a project, keeping the direct threads' README, and deleting a routine all go through no link. A thread working in a project subfolder moves files from the project's folder. The read-only rule no longer passes a backgrounded command, process substitution, or a variable that splits into options. A project agent's proposed skill can be kept in the library again. Routines say "doesn't ask" when they run as a Codex agent, in the app and `poly routine show`.
