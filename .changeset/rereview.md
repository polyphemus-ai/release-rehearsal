---
"@polyphemus/core": minor
"@polyphemus/daemon": minor
"@polyphemus/cli": minor
---

From a second review pass. Files an agent can write are now reached from a folder it can't replace, one folder at a time and never through a link, so a linked inbox, notes, skills, agents, routines, attachments or Downloads folder can't lead Polyphemus anywhere else. Accepting a routine names the version you read (the app sends it, and `poly routine accept` takes `--digest`); one changed since is refused. Commands whose options the shell fills in (`p=--pre; rg "$p" …`) ask first. Only the owner brings an agent into a conversation outside every project. An image is reached only through a message that actually carries it. Codex threads say "Doesn't ask" instead of "Asks first". A token refreshed while a call is made is masked like the one it replaced, and a call in a thread you can't see doesn't say who made it.
