---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

Signing in by hand is a real browser now: the pointer hovers, presses and releases where you put it, double-clicks work, any key goes straight to the page, and the page can be desktop-sized instead of phone-sized. A sign-in card in a thread closes when a sign-in for that site is kept, however it was done, and can always be dismissed.
