---
"@polyphemus/core": minor
"@polyphemus/daemon": minor
---

A new install offers the providers Polyphemus ships with instead of using them: nothing runs on one, and nobody can pick its models, until you accept it (Use it, in Models & providers). An install from before keeps every provider it had in use.
