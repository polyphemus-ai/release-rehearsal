---
"@polyphemus/cli": patch
---

The README says agents need Docker or Podman by default, and describes isolation as it is now. `poly pair` respects `POLYPHEMUS_TAILSCALE=off`, and the bundled fonts ship with their licenses.
