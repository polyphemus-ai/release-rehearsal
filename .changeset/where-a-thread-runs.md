---
"@polyphemus/daemon": patch
"@polyphemus/core": patch
---

A new thread shows what it will run on and where, before you send: a model chip (tap to pick another) and the project's isolation level (tap for what it means and where it's changed). A model that can't run isolated there is said on the draft. A thread's status line says the same: "Claude Code · claude-opus-5 · Isolated".
