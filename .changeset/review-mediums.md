---
"@polyphemus/core": minor
"@polyphemus/daemon": minor
---

More from the independent review. Grok Build, isolated, asks you before a command or an edit in a thread that asks first (it used to be allowed everything). A connection's errors and activity from a project's thread are shown only to people who can see that thread. An image can only be attached by whoever uploaded it, and is shown to anyone who can see a thread that carries it. Merging reads every page of a pull request's reviews and checks and stops if GitHub can't be asked; a run's branch is compared with the checked commit before it's pushed. The Browser connection works with only a worker's Chromium, addresses written as IPv4-mapped IPv6 are refused like the IPv4 they carry, Codex on a Mac says why it can't run isolated, and a library skill downloads no more than it keeps and installs only with its licence.
