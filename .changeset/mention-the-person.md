---
"@polyphemus/core": patch
"@polyphemus/daemon": patch
---

An agent @mentions you when it needs you, even when you're the only person in the thread, so a question or finished work reaches you as a notification instead of waiting unseen. Claude Code's own question form, which nobody could answer from a thread, is turned off: the agent asks in its reply instead. The command agents are given for `poly` keeps working after an update.
