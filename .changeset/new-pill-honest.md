---
"@polyphemus/daemon": patch
---

The chip above Home's list says "1 new" only when there's a new thread. When threads have only moved, because an agent is working in one, it says "Newer activity · tap to reorder" instead.
