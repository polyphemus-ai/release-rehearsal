---
"@polyphemus/daemon": patch
---

A conversation started from Direct stays outside every project, even with an agent that belongs to one. Agents that belong to a project aren't listed in Direct's "Say hello": you talk to them in their project, where their rules and memory are.
